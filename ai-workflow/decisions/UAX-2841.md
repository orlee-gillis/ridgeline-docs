# UAX-2841 - documentation decisions

## Summary

| Item | Decision | Reason |
| --- | --- | --- |
| Template | `feature-overview.md` | New page, and eventually a family of three: this overview, a troubleshooting page, and a how-to once mockups exist | Applying has its own prerequisite (write permission), its own failure states, and a 24-hour reversal window. Splitting deferred so session 15 produces a single-file diff |
| Page location | New page, not a section in `about-the-access-tab.md` | Applying has its own prerequisite (write permission), its own failure states, and a 24-hour reversal window |
| The sentence "Scope is never narrowed" | Split into two statements | Ridgeline does not move a grant to a lower scope level; the reader can select one |
| Write API behavior | Excluded from the draft | Documenting it would announce bulk apply one release before it ships |
| Structure | Restructured before the editing pass - see `../audits/restructure-apply-a-remediation.md` | The draft's capability list sat below the mechanism, reversal was treated as separate from the apply lifecycle, and eleven headings at one level gave the page no hierarchy |

## The sentence "Scope is never narrowed" in `about-the-access-tab.md`

A grant applies at one level of the scope ladder: **Organization**, **Workspace group**, **Workspace**,
**Project**, or **Resource**. Moving a grant to a lower level means it covers fewer resources than before.

| | Before | After this release |
| --- | --- | --- |
| Ridgeline's platform permissions | Read grants and activity logs | Read, plus change role definitions and the grants that attach roles - granted at connection time, or by re-consenting an existing connection |
| The recommendation | Applies at the grant's existing scope level, and cannot be altered | Applies at the same level, but the reader can select a lower level at the confirmation step |
| Is "Scope is never narrowed" accurate | Yes | Only for the recommendation, not for the change the reader applies |

**Files that state scope is never narrowed**

| File | Change required |
| --- | --- |
| `about-the-access-tab.md` | Reword "Scope is never narrowed. The replacement applies at the grant's original scope." Do not delete it - it is still accurate for the recommendation |
| Unused Access overview | Qualify "no additional platform permissions" - accurate for reading, not for writing |
| `style-guide.md`, publish-readiness checklist | The line "No remediation is described as narrowing scope or changing team membership" would fail a page that documents the reader's scope selection |
| `knowledge-base.md` §9, invariant 1 | States that no recommendation ever narrows scope and that documentation must never describe one doing so |

## Terminology: "an apply"

The page uses **apply** as a countable noun - "How an apply runs", "when the apply completes".
Decided to keep it: it matches the **Apply** button, and the alternatives ("an apply operation",
"when applying a recommendation fails") are wordier and force gerund headings.

Not added to the glossary. Both glossary copies are bundled inside frozen skills, and there is
no upstream copy yet. Belongs to the glossary reconciliation chore.


## Excluded from the draft

The assignee's comment on the story states that the write API accepts an array of grants while the
interface submits one. Documenting it would announce bulk apply one release before it ships.

## Open questions

**Blocking publication**

| Question | Owner |
| --- | --- |
| When does an applied change appear in the report and the grant table? A report showing pre-apply values is indistinguishable from a failed apply | M. Bell, engineer |
| Is **Apply** hidden or shown disabled when write permission is missing? | D. Reyes, PM - tie-break |
| The feature's final name. Title, sidebar label, and every inbound link depend on it | D. Reyes, PM, via marketing |
| Reader-facing names for the remediation wizard, the pre-write comparison step, and the history surface | P. Shah, design |
| Which role in Ridgeline can select **Apply**? A write path with no documented actor permission leaves the reader unable to tell whether they are blocked by their own role or by a missing platform permission | D. Reyes, PM |

**Not blocking**

| Question | Owner |
| --- | --- |
| What does the reader do once the 24-hour reversal window closes? | M. Bell, engineer |
| Why is the cap five updates, and does it rise for larger customers? | D. Reyes, PM |
| Will JIT grants ever be applicable? Raised twice by S. Okafor, never answered | D. Reyes, PM |
| The re-consent banner copy | P. Shah, design |

**Assigned to me: duplicate copies of `glossary.md` are not compared in CI**

| | |
| --- | --- |
| Issue | Two copies of `glossary.md`, one in each skill folder, so that each folder contains every file the skill references. No CI step compares them |
| Effect | An edit to one file is not propagated to the other, and a page drafted against the older copy follows outdated terminology rules without raising an error |
| Resolution | A CI step that runs `diff` on the two files. `diff` returns exit code 1 when they are not identical, which fails the step and blocks the merge. Both copies stay in place |

## Repo-wide items this page surfaced

The missing feature-overview genre in `audit-checklist.md`, the two template heading decisions,
and the three CI gates are tracked in `../TODO.md`.

## Changes made to the draft
| Change | Why |
| --- | --- |
| Removed "Who this is for" as a heading; folded it into the opening paragraphs | Awkward wording, adds unnecessary content |
| Moved "What you can do" above the mechanism section | The draft put the capability list below the mechanism. A reader needs to know what the feature lets them do before how it works |
| Nested role, scope, reversal, and timing as `###` under "How an apply runs" | Eleven headings at one level gave the page no hierarchy, and reversal was treated as separate from the apply lifecycle rather than part of it |
| Rebuilt Prerequisites as two tables with a `### Write permission` subheading | It needed to be easier to read and be permissions-focused |
| Kept the heading "Prerequisites" rather than renaming it | An important term and section to include|
| Changed heading forms from gerunds and imperatives to "How…" and "When…" | The heading forms made the page read as a how-to. It is a feature overview |
| Rewrote "When an apply fails" in full | Wasn't worded well nor clearly |
| Split "When the change appears" | Needed to make the cause-and-effect relationship clearer|
| Moved both `[SCREENSHOT]` placeholders out of the end-of-page stack into the sections they illustrate | The draft parked both at the end of the page. An image has to sit under the content it illustrates |
| Cut three restatements of the write-after-confirm fact from the numbered list | Steps 2, 3, and 4 each stated it separately |
