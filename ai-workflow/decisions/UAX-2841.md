# UAX-2841 - documentation decisions

## Genre
Claude automatically chose the "Feature Overview" template
## New page or existing page
Why this is its own page instead of a section on the Access tab page.

## The scope conflict
The Access tab page says scope is never narrowed (meaning a grant won't cover less than it did). Still true of
recommendations, not true of applies.
### Why this conflict exists
#### Before
- Ridgeline could read user grants (read-only) but not change them (would require the user granting Ridgeline 'write' permissions).
- Remediation recommendations were fixed, not flexible.
#### Now
- Ridgeline can change user grants -- so users need to approve this separately in order to select **Apply recommendation**.
- When confirming an **Apply recommendation**, users can choose a smaller area for a grant to cover.
#### Outcome ####
I need to review wherever permissions and scope are mentioned and make sure it's still accurate/change accordingly.

## What I left off the page
Internal facts I excluded, and the rule I excluded them under.

## Still open
The questions I'm sending out, and who owns each.
